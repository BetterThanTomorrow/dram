(ns hello-world.core)

(println "Hello ClojureScript World!")

(comment
  ;; Evaluate these forms by placing the cursor in them
  ;; and press Alt+Enter.
  
  (inc 1)
  
  (map inc [1 2 3])
  
  ;; See also the Calva command:
  ;; *Fire up the Getting Started REPL*
  )